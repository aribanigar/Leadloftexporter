{
  "version": "1.0.384",
  "zip": "/extensions/leadcaptura-extension-v1.0.384.zip",
  "notes": "Fixed the downloaded zip extracting as a scatter of loose folders/files (options/, content/, background/, manifest.json, ...) instead of one named folder — an earlier repackage had dropped the wrapping folder the zip is supposed to have. Every published zip now always unzips into a single \"LeadCaptura\" folder. Packaging itself is now a single automated step (npm run build:extension) so this can't drift again."
}
